@model IEnumerable<wardrobe.Models.Occassion>

@{
    ViewBag.Title = "Index";
}

<h2>Occassions</h2>

<p>
    @Html.ActionLink("Add New Occassion", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Occassions)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.Occassions)
        </td>

        @*Commented these options out because I do not want to allow user to alter the Occasions only add more to the list*@
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.OccassionsID })
            @*@Html.ActionLink("Details", "Details", new { id=item.OccassionsID }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.OccassionsID })*@
        </td>
    </tr>
}

</table>
